{
  "name": "jsonl-gazelle-screenshots",
  "private": true,
  "description": "Generates the README screenshots from the real webview code (no VS Code install required).",
  "scripts": {
    "generate": "node run-all.js"
  },
  "devDependencies": {
    "monaco-editor": "0.44.0",
    "playwright": "1.56.1"
  }
}
